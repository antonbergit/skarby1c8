
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Объект.ПоляДляПоискаПлатежей.Загрузить(Параметры.ПоляДляПоискаПлатежей.Выгрузить());
	
КонецПроцедуры


&НаКлиенте
Процедура ПриЗакрытии()

	СтруктураВозврата = Новый Структура;
	СтруктураВозврата.Вставить("ПоляДляПоискаПлатежей", Объект.ПоляДляПоискаПлатежей);
	ОповеститьОВыборе(СтруктураВозврата);

КонецПроцедуры

